# 🚀 Início Rápido

## Passo 1: Instalar Dependências

Execute no diretório raiz:

```bash
npm run install:all
```

Ou manualmente:

```bash
cd backend && npm install
cd ../frontend && npm install
```

## Passo 2: Iniciar o Backend

Em um terminal:

```bash
npm run dev:backend
```

O backend iniciará em `http://localhost:3001`

## Passo 3: Iniciar o Frontend

Em outro terminal:

```bash
npm run dev:frontend
```

O frontend iniciará em `http://localhost:3000`

## Passo 4: Acessar o Sistema

Abra seu navegador em: `http://localhost:3000`

## ✅ Pronto!

Agora você pode:
- Registrar novas frequências
- Visualizar no calendário
- Ver o total de horas do período
- Editar e excluir registros

## 📝 Nota

O banco de dados SQLite será criado automaticamente na primeira execução em `backend/database.sqlite`

